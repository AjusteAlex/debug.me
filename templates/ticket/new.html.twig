{% extends 'base.html.twig' %}

{% block title %}Nouveau ticket
{% endblock %}

{% block body %}

	<div class="bg-connexion-pattern w-full flex justify-center content-center">
		<div class="max-w-sm rounded overflow-hidden shadow-lg w-1/3 p-8">
			<div class="font-bold text-xl mb-8">Créer un ticket</div>
			{{ include('ticket/_form.html.twig') }}
		</div>
	</div>
	<a href="{{ path('app_ticket_index') }}">back to list</a>
{% endblock %}
