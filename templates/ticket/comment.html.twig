{% extends 'base.html.twig' %}

{% block title %}Nouveau commentaire
{% endblock %}

{% block body %}
	<div class="w-full flex justify-center content-center flex-col rounded pb-4 pt-2">
		<div class="flex content-center flex-row justify-between m-2">
			<a href="{{ path('app_ticket_index') }}" class="bg-transparent hover:bg-gradient-to-r from-pink to-purple text-primary-color font-semibold hover:text-white-transparent py-2 px-4 border border-primary-color hover:border-none rounded" style="overflow: hidden;">
				Retour
			</a>
		</div>
		<div class="flex content-center flex-row justify-between m-2">
			<div class="bg-connexion-pattern w-full flex justify-center content-center">
				<div class="max-w-sm rounded overflow-hidden shadow-lg w-full p-8">
					<div class="font-bold text-xl mb-8">Créer un commentaire</div>
					{{ include('ticket/_formComment.html.twig') }}
				</div>
			</div>
		</div>

	{% endblock %}
